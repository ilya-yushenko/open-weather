<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="searchViewStyle">@style/Widget.AppCompat.SearchView.mySearch</item>
    </style>

    <style name="Widget.AppCompat.SearchView.mySearch" parent="Widget.AppCompat.SearchView">
        <item name="android:background">@drawable/round_style</item>
    </style>

</resources>